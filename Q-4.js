Q-4
// Check if a number is positive, negative, or zero. 

let num = -9;

let result = (num > 0)
    ? "Positive number"
    : (num < 0)
        ? "Negative number"
        : "Zero";

console.log(result);